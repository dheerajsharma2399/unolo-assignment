version: '3.8'

services:
  backend:
    build: ./backend
    container_name: unolo_backend
    ports:
      - "3001:3001"
    volumes:
      - ./backend/data:/app/data
    environment:
      - PORT=3001
      - JWT_SECRET=development_secret_key
    restart: always

  frontend:
    build: ./frontend
    container_name: unolo_frontend
    ports:
      - "9006:80"
    depends_on:
      - backend
    restart: always
